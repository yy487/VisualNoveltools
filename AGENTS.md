# VN 工具项目规则

## 项目路径

- 每个新项目开始前，先从用户当前消息或项目子目录说明中确认“工具目录”和“实践目录”。
- 工具目录用于正式代码、稳定配置和必要文档；实践目录用于样本、探针、中间结果、回环产物和实验记录。
- 只使用用户为当前项目明确给出的两个目录，不沿用其他游戏、引擎或会话的路径和假设。
- 任一路径未给出或含义不明确时，先向用户确认，再创建文件或修改代码。
- 写入前验证目标路径位于用户指定目录内。不要把样本、游戏资源或实践产物复制进工具目录，除非用户明确要求。

## 完成与提交模式

- 默认先在实践目录分析、开发和验证；不要把未完成的实验稿直接写入工具目录。
- 工具达到当前任务验收标准后，把正式代码、稳定配置和必要文档复制到工具目录，并从工具目录中的最终副本再次运行相关测试或回环验证。
- 验证通过后直接创建 Git 提交，不必再次询问用户；用户明确要求“不提交”时除外。提交只代表本地 commit，未经用户明确要求不得 push。
- 只精确暂存当前工具目录内本次完成的文件，以及确属本次任务的项目规则文件。不得使用会混入其他改动的全量暂存命令。
- 实践目录、样本、游戏资源、中间文件、生成产物和本地 `.agents/skills/` 不得进入提交。
- 提交前检查 staged diff、运行适用测试并确认未包含用户已有改动。若验证失败、Git 身份不可用、工具目录不在当前仓库，或无法区分本次文件与既有改动，则停止提交并说明原因。

## VN 本地化工作流

- 分析封包、脚本 VM、字节码、字符串表、文本提取/注入、重封包或补丁验证时，使用本地 skill：`.agents/skills/gal-vn-bytecode-localization-json-workflow/SKILL.md`（存在时）。
- 翻译中间格式统一使用 UTF-8 JSON；`scr_msg` 保存原文且不可修改，`message` 是唯一默认写回字段，有明确说话人时才输出 `name`。
- 正式提取必须来自已确认的结构、指令流、IR、字符串表或资源表。启发式扫描只能用于探索和统计，不能冒充最终解析器。
- 修改前保留定位与校验信息；注入前校验原文；存在变长写回时，完整处理字符串引用、跳转、指针、大小和外层文件表。
- 零修改回环必须优先验证；声称逐字节一致时，必须提供实际哈希或二进制比较结果。

## 反汇编规则

- `asm.txt` 仅用于语义反汇编、汇编和零突变验证，不作为翻译中间格式，因此不与 JSON 规则冲突。
- 反汇编/汇编任务还应读取 skill 内的 `references/vm-roundtrip.md`。
- “`opcode.py`、`disassembler.py`、`assembler.py` 三个 Python 文件”是用户明确要求最小工具时的交付形态；一般项目仍按共享逻辑、提取、注入、封包和验证职责拆分。
- 未知结构、opcode 和数据必须明确报错或无损保留，不得静默丢弃。

## 文档与范围

- 每个工具目录保留一份简洁 README，说明适用样本、命令、编码、JSON 字段、注入策略、验证结果和已知限制。
- 优先遵循用户当前要求和更深层目录中的 `AGENTS.md` / `CLAUDE.md`；当前项目已证实的事实优先于通用模板。
- 不修改与当前项目无关的文件，不清理用户已有改动。
