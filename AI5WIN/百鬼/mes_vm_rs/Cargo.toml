[package]
name = "mes_vm_rs"
version = "0.1.0"
edition = "2021"

[dependencies]
encoding_rs = "0.8"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
opt-level = 3
lto = "thin"
