# 项目档案：少女たちは荒野を目指す

## 路径与所有权

- 样本位置：`E:\GAL\ianzhong\mes`
- 开发工作目录：`H:\IDA-PRO-MCP\work\ianzhong-mes-text`
- 正式工具目录：`E:\GAL\ianzhong\tools\ianzhong-mes-text`
- 源 `mes` 目录只读；提取和注入输出必须写入新文件或新目录。
- 实现语言：Rust 2024 edition。

## 归档与脚本

- 外层档案：manifest 标记为 `ACV legacy DAT`，共 220 项；归档解包/封包不在本工具范围。
- 引擎/运行时：未确认。
- 脚本：220 个明文 `.txt`，总计 2,284,198 字节（不含 manifest）。
- 编码：CP932；全部 220 个样本完成严格解码/重编码字节回环。
- 换行：全部为 LF；没有 NUL。
- 命令、标签和注释行按已确认 skip 规则排除。
- 明文脚本可整体重建，因此脚本层支持变长注入；外层归档的 size、offset、压缩和文件表由独立封包器负责。

## 用户确认的正文与姓名规则

确认日期：2026-07-31。

- 采用用户提供的 11、12、21、22、23、24、25、30、31、32、42 号规则优先级。
- `【文太郎,S000...】正文`：逗号前为 `name`，`】` 后为 `message`。
- `【砂雪@？？？,S001...】正文`：`@` 后、逗号前为 `name`。
- `【砂雪@,S001...】正文`：`@` 前为 `name`。
- `【姓名】正文`：括号内为 `name`。
- 其他非空、非 skip 行为无名 `message`。
- `name` 允许翻译并写回；`_scr_name` 保存并校验原名。
- `scr_msg` 永远不可编辑；只把 `message` 写回正文。
- 姓名中的结构分隔符 `【】@,` 禁止改入。

样本统计：

- 11：2 条。
- 12：85 条。
- 21：18,339 条。
- 23：18 条。
- 42：6,261 条。
- 22、24、25、30、31、32：真实样本 0 条，按给定语法实现并以合成测试验证结构回环。

## 选项

`confirmed-structure`：

- 选项载荷均为引号字符串 `"显示文本,*目标标签"`。
- 使用载荷中最后一个 `,*` 分隔正文与目标，目标不可编辑。
- 54 个内联 `SELECT` 槽。
- 6 个 `SELECT_INIT` 槽。
- 6 个动态 `$g_select` 赋值槽，分别位于 `011_A_140419_0.txt` 和 `074_B_141017_0.txt`。
- 合计 66 个选项槽；源样本没有选项内双引号或控制符。

JSON 中每个选项独立成条目，保留 `_file`、`_index`、`_line`、`_offset`、`_size`、`_type=choice`、`_opcode`、`_target`、`_choice_index` 和 `_encoding`。

## 控制符

- 原始形式：`[n]`。
- 出现位置：正文内，共 8,421 次。
- 结构含义：显示换行；脚本文件本身仍使用 LF 分行。
- 提取：原样保留在 `scr_msg` 和 `message`。
- 注入：允许从正文删除一个或多个方括号控制符（包括全部删除）以测试自动换行；保留的控制符必须是源序列的有序子序列，不得新增或乱序；禁止换成真实 CR/LF。
- 语义确认级别：`confirmed-structure`（显示效果未单独动态调试）。

## 验证与限制

- 要求 `source -> extract -> unchanged inject` 后源树相对路径与 SHA-256 完全一致。
- 修改验证覆盖正文长短变化、姓名写回、选项写回、重复文本按 `_index` 定位、控制符和 CP932 失败。
- 未知命令和未提取行由目录复制及局部 span 替换无损保留。
- 外层 ACV 重建、字体字库和游戏内显示不在本工具范围，运行时语义均标记为未确认。
