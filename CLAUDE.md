# VN 工具项目说明

## 开始新项目

用户会为每个新项目指定两个路径：

- **工具目录**：正式工具代码、稳定配置和必要文档的唯一写入范围。
- **实践目录**：样本、探针、中间文件、实验输出和回环验证产物的工作范围。

开始工作前必须确认这两个路径。不得沿用其他项目的路径、opcode、密钥、编码、结构或游戏假设；缺少任一路径时先询问用户。写入前确认目标仍位于相应指定目录内。

## 默认完成流程

1. 在实践目录完成分析、实现、样本测试和回环验证。
2. 达到当前任务验收标准后，只把正式代码、稳定配置和必要文档复制到工具目录。
3. 从工具目录中的最终副本重新运行适用测试，避免提交未经验证的复制结果。
4. 精确暂存工具目录中本次完成的文件，检查 staged diff 后直接创建本地 Git commit，无需再次询问。
5. 不自动 push；只有用户明确要求时才推送。

实践目录、游戏资源、样本、中间输出、生成产物和本地 `.agents/skills/` 一律不提交。不得全量暂存而混入其他项目或用户已有改动。若测试失败、Git 身份缺失、工具目录不属于当前仓库，或无法可靠区分本次改动，则不要提交，并向用户说明阻塞原因。

## 核心要求

1. VN 翻译主流程使用 UTF-8 JSON，不使用 txt/DSAT 作为翻译中间格式。
2. `scr_msg` 仅保存原文并用于定位校验；`message` 是默认写回字段；只有明确存在说话人时才输出 `name`。
3. 文本提取以结构化解析、VM 指令流、IR、字符串表或资源表为依据。启发式扫描只能辅助研究。
4. 注入必须校验来源，并按实际格式处理编码、槽长度、字符串重定位、跳转、指针、大小和封包表。
5. 反汇编与汇编必须支持零修改回环；只有实际二进制比较或哈希一致后才能声称逐字节还原。
6. 未识别指令或数据不得静默跳过。不能确认语义时，明确记录证据、风险和下一步验证方法。

## 规则协调

- `asm.txt` 是语义反汇编和回环验证格式，不是翻译格式；翻译导入导出仍使用 JSON。
- 三文件结构（`opcode.py`、`disassembler.py`、`assembler.py`）仅在用户明确要求最小交付时强制采用；否则按职责拆分共享逻辑、提取、注入、封包和验证代码。
- 本地 Codex skill 位于 `.agents/skills/gal-vn-bytecode-localization-json-workflow/`，该目录已被 Git 忽略，不得提交到 GitHub。
- 完整的 VM 语义反汇编规范保存在该 skill 的 `references/vm-roundtrip.md`。
- 更深层目录中的项目说明和用户当前指令优先；通用模板不得覆盖已经由样本、可执行文件或测试确认的事实。

## 交付与验证

- 工具目录内提供简洁 README，记录适用范围、命令、编码、格式依据、JSON 约定、注入能力、验证结果和限制。
- 实践目录内进行样本分析、零修改回环、翻译写回和封包验证，避免污染正式工具目录。
- 不修改当前项目范围外的文件，不清理或覆盖用户已有改动。
